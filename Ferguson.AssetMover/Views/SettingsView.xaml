<UserControl x:Class="Ferguson.AssetMover.Client.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:Ferguson.AssetMover.Client.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid
        extensions:GridExtensions.RowDefinitions="50, *, *" Margin="12">

        <Button Content="Import settings from file" Width="200" Margin="6" Command="{Binding OpenSettingsFileCommand}" HorizontalAlignment="Left"></Button>
        
        <Grid Grid.Row="1"
        extensions:GridExtensions.RowDefinitions="30, 30, *"
        extensions:GridExtensions.ColumnDefinitions="150, *" Margin="12">

            <TextBlock Text="Filepath" Foreground="{StaticResource SubtleColorBrush}" />
            <TextBlock Grid.Column="1" Text="{Binding FilePath}" />
            <TextBlock Grid.Row="1" Text="ConsigmentLocation" Foreground="{StaticResource SubtleColorBrush}" />
            <TextBlock Grid.Column="1" Text="{Binding FilePath}" />
        </Grid>
    </Grid>
</UserControl>
